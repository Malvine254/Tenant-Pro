<div class="form-group"><label for="property-neighbourhood">Neighbourhood / estate</label><input id="property-neighbourhood" name="neighbourhood" maxlength="100" value="{{ old('neighbourhood', $property->neighbourhood ?? '') }}" placeholder="For example, Kilimani">@error('neighbourhood')<div class="form-error">{{ $message }}</div>@enderror</div>
<div class="form-group"><label for="property-area-notes">Local area guide</label><textarea id="property-area-notes" name="area_notes" rows="4" maxlength="2000" placeholder="Describe nearby transport stops, shops and services, and what renters should check on a visit. Only include facts you can confirm.">{{ old('area_notes', $property->area_notes ?? '') }}</textarea><small>Published as manager-supplied notes on the neighbourhood page. Avoid personal details and unsupported safety claims.</small>@error('area_notes')<div class="form-error">{{ $message }}</div>@enderror</div>
